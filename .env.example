# ---------------------------------------------------------------------------
# Signal — variables de entorno
# ---------------------------------------------------------------------------

# Postgres. Este producto vive en el esquema "signal" del proyecto compartido.
# Desde Prisma 7 esta cadena la leen tanto prisma.config.ts (migraciones) como
# el driver adapter del cliente.
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
